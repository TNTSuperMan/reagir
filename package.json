{
  "name": "reagir",
  "module": "src/index.ts",
  "type": "module",
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "scripts": {
    "build": "tsc && bun build.ts",
    "build-sample": "bun build sample/sample.ts --minify --outdir sample"
  }
}